import { AppBar } from "@mui/material";

const pages = ['Vehicles', 'Launches', 'Human SpaceFlight', 'RideShare', 'Starlink', 'Starshield', 'Company', 'Shop'];

export const NavBar = () => {

    return (
        <>
        <AppBar>
            
        </AppBar>
        </>
    )

}